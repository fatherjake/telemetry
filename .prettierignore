node_modules
dist
coverage
*.min.js
*.min.mjs

# Generated by the build from .claude/skills, so formatting it here would be
# undone on the next `npm run build` and would drift from its source.
skills/

# The schema is SQL that prettier has no parser for, and the raw archive is
# append-only evidence - reformatting a captured OTLP payload would corrupt it.
*.sql
*.jsonl
*.db

package-lock.json
